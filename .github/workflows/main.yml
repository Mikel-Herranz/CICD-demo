name: Primer workflow

on: push

jobs:
  validar:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: echo "Hola chat actions"

      - name: Bajar codigo
        uses: actions/checkout@v5

      - name: Probar codigo
        run: python3 main.py

      - name: Crear imagen docker
        run: docker build -t mikelh/holachat .

      - name: probar contenedor docker
        run: docker run mikelh/holachat

        
    
    
